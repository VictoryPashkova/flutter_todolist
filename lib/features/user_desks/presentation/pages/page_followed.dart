import 'package:flutter/material.dart';

class PageFollowed extends StatelessWidget {
  const PageFollowed({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'In progress',
        style: TextStyle(
          fontSize: 24,
          color: Colors.black,
        ),
      ),
    );
  }
}
